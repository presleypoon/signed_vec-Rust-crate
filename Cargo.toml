[package]
name = "signed_vec"
version = "1.0.0"
edition = "2024"
description = "A vector data structure that supports negative indexing in Rust"
license = "Apache-2.0"
repository = "https://github.com/your-username/signed_vec"

[dependencies]
